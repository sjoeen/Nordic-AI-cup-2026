[pytest]
testpaths = tests
timeout = 300
markers =
    slow: loads real models or audio; skipped unless RUN_SLOW=1
    integration: runs several stages together with fakes
addopts = -q -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
